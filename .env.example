# ===================================
# ePANEN - Environment Variables for Deployment
# ===================================

# ===================================
# SUPABASE DATABASE (Required)
# ===================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===================================
# AI SERVICE (OpenRouter)
# ===================================
AI_API_URL=https://openrouter.ai/api/v1
AI_API_KEY=sk-or-v1-your-key-here
AI_MODEL=x-ai/grok-4.1-fast
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7

# ===================================
# JWT SECRET
# ===================================
JWT_SECRET=generate-random-secret-here-min-32-chars

# ===================================
# FRONTEND URL (for CORS)
# ===================================
FRONTEND_URL=https://your-domain.com

# ===================================
# POSTGRES (Optional - Only if NOT using Supabase Cloud)
# ===================================
# POSTGRES_PASSWORD=epanen123

# ===================================
# DEPLOYMENT NOTES:
# ===================================
#
# 1. Persiapkan VPS:
#    - OS: Ubuntu 20.04 or 22.04 recommended
#    - RAM: Min 1GB (2GB recommended)
#    - Storage: 20GB+
#
# 2. Install Docker & Docker Compose on VPS:
#    curl -fsSL https://get.docker.com -o get-docker.sh
#    sh get-docker.sh
#    sudo usermod -aG docker $USER
#    sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
#    sudo chmod +x /usr/local/bin/docker-compose
#
# 3. Upload project ke VPS:
#    - Zip folder epanen-web
#    Upload via SCP/SFTP ke VPS
#    Atau use git: git clone <your-repo>
#
# 4. SSH ke VPS dan jalankan:
#    cd epanen-web
#    cp .env.example .env
#    nano .env  # Edit environment variables
#    docker-compose up -d
#
# 5. Setup Domain & SSL (Opsional):
#    - Point domain ke VPS IP
#    - Install certbot: sudo apt update && sudo apt install certbot
#    - sudo certbot --nginx -d yourdomain.com
#
# 6. Check logs:
#    docker-compose logs -f
#
# 7. Stop services:
#    docker-compose down
#
# ===================================
